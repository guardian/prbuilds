PR build results:

results:
{% for name, result in results.items() -%}
{{name}} {{result}}
{% endfor %}
    
screenshots
{% for artifact in artifacts -%}
{% if "screenshot" in artifact -%}
{{link(artifact)}}
{% endif -%}
{%endfor%}

exceptions
{{ results["exceptions"]["raw_output"].count("EXCEPTION") }}

others
{% for artifact in artifacts -%}
{% if "screenshot" not in artifact -%}
{{link(artifact)}}
{% endif -%}
{%endfor%}
    
-automated message
